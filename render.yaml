services:
  - type: web
    name: ai-job-agent
    env: docker
    plan: free
    dockerCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 8000
      - key: SERPAPI_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
